doctype html
html
	head
		title="login Google"
	body
		div
			h1 Sign in
			form(action='http://localhost:6000/logged', method='GET')
				p First name: 
					input(type='text', name='first_name')
				p Last name: 
					input(type='text', name='last_name')
				button(type='submit') Log in imput#checkBox(type='checkbox', name="remember")
				label(for='remember') Remember me